## Packages
react-markdown | Rendering markdown in chat messages
remark-gfm | Github flavored markdown for tables and strikethrough
react-syntax-highlighter | Code block syntax highlighting
@types/react-syntax-highlighter | Types for syntax highlighter
clsx | Utility for constructing className strings
tailwind-merge | Utility for merging tailwind classes

## Notes
- SSE via fetch is used for streaming POST /api/conversations/:id/messages
- Terminal-inspired aesthetic requires font-mono broadly applied to the layout
- Uses full-screen layout without body scroll
